{
  "name": "yyssearch",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "npm run clean:build && webpack",
    "dist": "npm run clean:dist && webpack",
    "server": "npm run build && webpack-dev-server --open",
    "clean:dist": "rm -rf dist",
    "clean:build": "rm -rf build",
    "clean": "npm run clean:dist && npm run clean:build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KCN",
  "license": "GPL-3.0",
  "devDependencies": {
    "autoprefixer": "^6.7.7",
    "babel-core": "^6.24.1",
    "babel-loader": "^7.0.0",
    "copy-webpack-plugin": "^4.0.1",
    "css-loader": "^0.28.0",
    "extract-text-webpack-plugin": "^2.1.0",
    "file-loader": "^0.11.1",
    "html-webpack-plugin": "^2.28.0",
    "null-loader": "^0.1.1",
    "postcss-loader": "^1.3.3",
    "raw-loader": "^0.5.1",
    "style-loader": "^0.16.1",
    "webpack": "^2.4.1"
  }
}
